<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Tema del splash screen para Android 12+ -->
    <style name="Theme.App.Start" parent="Theme.SplashScreen">
        <item name="windowSplashScreenAnimatedIcon">@drawable/hangman_logo</item>
        <item name="windowSplashScreenBackground">@color/backgroundDark</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.Hangman</item>
        <!-- Solo atributos compatibles -->
        <item name="android:windowSplashScreenBrandingImage">@drawable/hangman_logo</item>
    </style>

    <!-- Tema para el resto de la aplicación -->
    <style name="Base.Theme.Hangman" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/backgroundDark</item>
    </style>
</resources>
